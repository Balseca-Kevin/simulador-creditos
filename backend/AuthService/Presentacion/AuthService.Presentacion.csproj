<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AuthService.Presentacion</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- No referencia Estructura: los controladores no conocen la base de datos. -->
    <ProjectReference Include="..\Aplicacion\AuthService.Aplicacion.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Da acceso a los tipos de ASP.NET Core sin convertir la biblioteca en una aplicacion web. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <!-- El SDK web importa estos espacios de nombres por omision; una
         biblioteca de clases no, asi que se declaran aqui. -->
    <Using Include="Microsoft.AspNetCore.Http" />
    <Using Include="Microsoft.AspNetCore.Mvc" />
  </ItemGroup>

</Project>
