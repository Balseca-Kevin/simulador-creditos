<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>CreditService.Aplicacion</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Dominio\CreditService.Dominio.csproj" />
  </ItemGroup>

  <!--
    Sin paquetes: el motor de amortizacion es matematica pura sobre decimal.
    Que esta capa no necesite nada externo es justo lo que permite probarla
    sin base de datos ni servidor.
  -->

</Project>
